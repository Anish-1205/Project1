import React, { useState, useEffect } from "react"; import axios from "axios"; import { useNavigate } from "react-router-dom"; function ResultPage({ attemptId }) { const [result, setResult] = useState(null); const [loading, setLoading] = useState(true); const navigate = useNavigate(); useEffect(() => { axios.get(`http://localhost:8080/api/quizzes/attempt/${attemptId}/result`).then(r => setResult(r.data)).finally(() => setLoading(false)); }, [attemptId]); if (loading || !result) return <div>Loading...</div>; return <div style={{maxWidth: "900px", margin: "0 auto", padding: "40px", textAlign: "center"}}><h1>Quiz Completed!</h1><div style={{display: "flex", justifyContent: "center", gap: "20px", alignItems: "center", margin: "30px 0"}}><div style={{fontSize: "4rem", fontWeight: "bold", color: "#3498db", background: "#ecf8fd", width: "120px", height: "120px", borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center"}}>{result.score >= 80 ? "A" : result.score >= 60 ? "B" : "C"}</div><div style={{fontSize: "2rem", fontWeight: "bold"}}>{result.score}%</div></div><div style={{display: "grid", gridTemplateColumns: "repeat(2, 1fr)", gap: "20px", margin: "30px 0"}}><div>✓ Correct: {result.correctAnswers}/{result.totalQuestions}</div><div>✗ Incorrect: {result.incorrectAnswers}</div></div><button onClick={() => navigate("/")} style={{padding: "12px 30px", background: "#3498db", color: "white", border: "none", borderRadius: "6px", cursor: "pointer", marginTop: "20px"}}>Back to Home</button></div>; } export default ResultPage;
