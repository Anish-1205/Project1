import React from "react"; import { BrowserRouter as Router, Routes, Route, useParams } from "react-router-dom"; import QuizListing from "./components/QuizListing"; import QuizAttempt from "./components/QuizAttempt"; import ResultPage from "./components/ResultPage"; function App() { return <Router><Routes><Route path="/" element={<QuizListing />} /><Route path="/quiz/:quizId" element={<QuizAttemptWrapper />} /><Route path="/result/:attemptId" element={<ResultPageWrapper />} /></Routes></Router>; } function QuizAttemptWrapper() { const { quizId } = useParams(); return <QuizAttempt quizId={quizId} />; } function ResultPageWrapper() { const { attemptId } = useParams(); return <ResultPage attemptId={attemptId} />; } export default App;
